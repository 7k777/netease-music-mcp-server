services:
  - type: web
    name: netease-music-mcp
    env: python
    buildCommand: pip install --upgrade pip && pip install fastapi httpx cryptography uvicorn
    startCommand: python netease_mcp_server.py
